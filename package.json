{
  "name": "shrinkr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "dev": "npm run dev --workspace @shrinkr/api & npm run dev --workspace @shrinkr/web",
    "dev:api": "npm run dev --workspace @shrinkr/api",
    "dev:web": "npm run dev --workspace @shrinkr/web",
    "build": "npm run build --workspace @shrinkr/api && npm run build --workspace @shrinkr/web",
    "lint": "npm run lint --workspace @shrinkr/api && npm run lint --workspace @shrinkr/web",
    "typecheck": "npm run typecheck --workspace @shrinkr/api && npm run typecheck --workspace @shrinkr/web",
    "test": "npm run test --workspace @shrinkr/api && npm run test --workspace @shrinkr/web",
    "verify:all": "npm run lint && npm run typecheck && npm test && npm run build"
  },
  "devDependencies": { "typescript": "^5.6.3" }
}
